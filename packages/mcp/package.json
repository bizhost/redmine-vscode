{
  "name": "@bizhost/redmine-mcp",
  "version": "0.1.1",
  "description": "Redmine MCP 서버 — 일감 조회/상태변경/댓글/내보내기",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/bizhost/redmine-vscode",
    "directory": "packages/mcp"
  },
  "bin": {
    "redmine-mcp": "dist/server.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "build:mcpb": "npm run build && node build-mcpb.mjs",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@redmine-tools/core": "*",
    "@types/node": "^26.1.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "zod": "^3.24.0"
  }
}
